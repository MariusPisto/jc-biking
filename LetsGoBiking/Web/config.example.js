// Example config for development. Do NOT commit your real API key.
// Copy this file to config.js and set ORS_API_KEY there for local testing
// Alternatively, serve the key from a backend and fetch it at runtime.

window.APP_CONFIG = {
  ORS_API_KEY: 'YOUR KEY HERE',
};
